#include "layers.h"

namespace ginger {
	/*
	void Layers::update() {
		for (MapImageIterator it = images.begin(); it != images.end(); ++it) {
			sf::Vector2f pos = it->second.sprite.getPosition();
			pos.x += globalOffsetX;
			pos.y += globalOffsetY;
		}

		for (MapTilesetIterator it = tiles.begin(); it != tiles.end(); ++it) {
			sf::Vector2f pos = it->second.tiles.sprite.getPosition();
			pos.x += globalOffsetX;
			pos.y += globalOffsetY;
		}

		for (MapObjectIterator it = staticObjects.begin(); it != staticObjects.end(); ++it) {
			rt.draw(*it);
		}
	}
	*/
}